# openapi-typescript (a dev-only codegen cli) declares a peer of typescript@^5,
# but the project uses typescript 6. the mismatch is harmless for a codegen tool,
# so allow installs to proceed without manual --legacy-peer-deps each time.
legacy-peer-deps=true
